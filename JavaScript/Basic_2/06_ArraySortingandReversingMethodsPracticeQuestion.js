// Write a JavaScript statement to sort the elements of the array [3, 1, 2, 4] in ascending order.

const num = [3,1,2,4]
num.sort()
console.log(num)


// // Write a JavaScript statement to reverse the order of the elements in the array [1, 2, 3, 4, 5].

const num2 = [1, 2, 3, 4, 5]
num2.reverse()
console.log(num2)

// // // What is the output of the following code: let numbers = [4, 2, 7, 1, 3]; numbers.sort(); console.log(numbers);

// output is [1,2,3,4,7]

// // // What is the output of the following code: let numbers = [1, 2, 3, 4, 5]; numbers.reverse(); console.log(numbers);

// output is [5,4,3,2,1]